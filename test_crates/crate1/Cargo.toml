[package]
name = "crate1"
version = "0.1.0"
authors = ["Paul Dorehill"]
edition = "2018"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "=0.2.67"
web-sys = { version = "0.3", features = ["console"] }